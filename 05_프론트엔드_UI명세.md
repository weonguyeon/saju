# 프론트엔드 UI 명세서

## 📌 개요

3개의 HTML 템플릿과 1개의 CSS 파일로 구성됩니다.

---

## 🎨 디자인 시스템

### CSS 변수 (style.css)

```css
:root {
    --bg-color: #fdfbf7;           /* 크림색 배경 */
    --card-bg: rgba(255, 255, 255, 0.65);
    --card-border: rgba(255, 255, 255, 0.8);
    --text-main: #333333;
    --text-muted: #666666;
    
    /* 파스텔 팔레트 */
    --primary: #a78bfa;            /* 소프트 보라 */
    --accent: #f472b6;             /* 소프트 핑크 */
    --success: #34d399;            /* 민트 그린 */
    --danger: #fbbf24;             /* 웜 옐로우 */
    
    --glass-shadow: 0 8px 32px 0 rgba(74, 58, 237, 0.1);
}
```

### 오행 색상

```css
.wood { color: #16a34a !important; }   /* 녹색 */
.fire { color: #ea580c !important; }   /* 주황 */
.earth { color: #d97706 !important; }  /* 갈색 */
.metal { color: #475569 !important; }  /* 회색 */
.water { color: #2563eb !important; }  /* 파랑 */
```

### 그라데이션 텍스트

```css
.text-gradient {
    background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
}
```

---

## 📄 템플릿 구조

### 1. index.html (입력 폼)

**구조**:
```html
<body>
    <div class="container">
        <div class="glass-card">
            <!-- 로고 -->
            <i class="fa-solid fa-moon text-gradient"></i>
            <h1>별하 (Byeolha)</h1>
            <p>당신의 운명을 비추는 별</p>
            
            <!-- 폼 -->
            <form action="/loading" method="POST">
                <input name="name" placeholder="홍길동">
                <input type="radio" name="gender" value="male"> 남성
                <input type="radio" name="gender" value="female"> 여성
                <input type="date" name="birth_date">
                <input type="time" name="birth_time">
                <button class="btn-primary">운세 확인하기</button>
            </form>
        </div>
    </div>
</body>
```

**폼 필드**:
| 필드명 | 타입 | 필수 | 설명 |
|--------|------|------|------|
| name | text | ✓ | 이름 |
| gender | radio | ✓ | male/female |
| birth_date | date | ✓ | 양력 생년월일 |
| birth_time | time | ✓ | 태어난 시간 |

---

### 2. loading.html (로딩 화면)

**구조**:
```html
<body style="background: #0f172a;">
    <!-- 스피너 -->
    <div class="mystic-spinner"></div>
    
    <!-- 순환 메시지 -->
    <div id="loading-msg">하늘의 별을 읽고 있습니다...</div>
    <div class="sub-text">잠시만 기다려 주세요...</div>
    
    <!-- 숨겨진 자동 제출 폼 -->
    <form id="auto-form" action="/result" method="POST" style="display: none;">
        {% for key, value in data.items() %}
        <input type="hidden" name="{{ key }}" value="{{ value }}">
        {% endfor %}
    </form>
</body>
```

**순환 메시지** (3초 간격):
```javascript
const messages = [
    "하늘의 별을 읽고 있습니다...",
    "당신의 사주 원국을 해독 중입니다...",
    "10년의 대운 흐름을 살피고 있습니다...",
    "깊이 있는 심층 조언을 갈무리하고 있습니다...",
    "당신의 인생 지도를 완성 중입니다..."
];
```

**자동 제출**:
```javascript
setTimeout(() => {
    document.getElementById('auto-form').submit();
}, 300);
```

---

### 3. result.html (결과 화면)

**전체 구조**:
```html
<body>
    <div class="container">
        <!-- 헤더 -->
        <header class="result-header">
            <h2>별하 (Byeolha)</h2>
            <h1>{{ name }}님의 심층 분석 결과</h1>
        </header>
        
        <!-- 사주 원국표 -->
        <div class="glass-card">
            <table class="saju-table">
                <thead>
                    <tr>
                        <th>구분</th>
                        <th>시주 (Hour)</th>
                        <th>일주 (Day)</th>
                        <th>월주 (Month)</th>
                        <th>년주 (Year)</th>
                    </tr>
                </thead>
                <tbody>
                    <tr><!-- 천간 행 --></tr>
                    <tr><!-- 지지 행 --></tr>
                </tbody>
            </table>
        </div>
        
        <!-- 탭 네비게이션 -->
        <div class="tabs">
            <button onclick="openTab(event, 'basic')">기본 해석</button>
            <button onclick="openTab(event, 'ohaeng')">오행 & 십성</button>
            <button onclick="openTab(event, 'gmhs')">인생의 흐름</button>
            <button onclick="openTab(event, 'daewoon')">대운 (10년)</button>
        </div>
        
        <!-- 탭 컨텐츠 -->
        <div id="basic" class="tab-content active">...</div>
        <div id="ohaeng" class="tab-content">...</div>
        <div id="gmhs" class="tab-content">...</div>
        <div id="daewoon" class="tab-content">...</div>
    </div>
</body>
```

---

## 📊 사주 원국표 상세

```html
<table class="saju-table">
    <thead>
        <tr>
            <th style="width:10%">구분</th>
            <th style="width:22.5%">시주 (Hour)</th>
            <th style="width:22.5%">일주 (Day)</th>
            <th style="width:22.5%">월주 (Month)</th>
            <th style="width:22.5%">년주 (Year)</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td class="label-cell">천간<br><small>(Stem)</small></td>
            <td class="{{ pillars.hour.gan_element }}">
                <span class="hanja">{{ pillars.hour.gan }}</span>
                <div class="ten-god">{{ interp.ten_gods.hour.gan }}</div>
            </td>
            <td class="{{ pillars.day.gan_element }} me-cell">
                <span class="hanja">{{ pillars.day.gan }}</span>
                <div class="ten-god">나 (Master)</div>
            </td>
            <!-- 월주, 년주 동일 패턴 -->
        </tr>
        <tr>
            <!-- 지지 행 동일 패턴 -->
        </tr>
    </tbody>
</table>
```

---

## 🔘 탭 컨텐츠 상세

### 탭 1: 기본 해석 (id="basic")

```html
<!-- 평생사주 총평 -->
<div class="glass-card" style="border-left: 5px solid var(--primary);">
    <h3><i class="fa-solid fa-crown"></i> 평생사주 총평</h3>
    <p>{{ interp.total_summary }}</p>
</div>

<!-- 오늘의 운세 -->
<div class="glass-card">
    <h3><i class="fa-solid fa-star"></i> 오늘의 운세</h3>
    <p>{{ interp.today_luck.date }} {{ interp.today_luck.pillar }}</p>
    <h4>{{ interp.today_luck.title }}</h4>
    <p>{{ interp.today_luck.desc }}</p>
</div>

<!-- 핵심 성향 -->
<div class="glass-card">
    <h3><i class="fa-solid fa-fingerprint"></i> 핵심 성향 (인품)</h3>
    <p>{{ interp.personality_deep }}</p>
</div>

<!-- 3열 그리드: 재물운, 사회적 적성, 애정운 -->
<div style="display: grid; grid-template-columns: repeat(3, 1fr);">
    <div class="glass-card">
        <h3><i class="fa-solid fa-coins"></i> 재물운 전략</h3>
        <p>{{ interp.wealth_strategy }}</p>
    </div>
    <div class="glass-card">
        <h3><i class="fa-solid fa-briefcase"></i> 사회적 적성</h3>
        <p>{{ interp.social_analysis }}</p>
    </div>
    <div class="glass-card">
        <h3><i class="fa-solid fa-heart"></i> 애정운</h3>
        <p>{{ interp.love_romance }}</p>
    </div>
</div>
```

### 탭 2: 오행 & 십성 (id="ohaeng")

```html
<!-- 2열 그리드 -->
<div class="dashboard-grid" style="grid-template-columns: 1fr 1fr;">
    <!-- 레이더 차트 -->
    <div class="glass-card">
        <h3>오행 균형 (Five Elements)</h3>
        <canvas id="ohaengChart"></canvas>
    </div>
    
    <!-- 밸런스 분석 -->
    <div class="glass-card">
        <h3><i class="fa-solid fa-scale-balanced"></i> 밸런스 분석</h3>
        <p>{{ interp.ohaeng_analysis.balance_text }}</p>
        <div class="advice-box">
            <h4>맞춤 조언</h4>
            <p>{{ interp.advice }}</p>
        </div>
    </div>
</div>

<!-- 오행 상세 분포 -->
<div class="glass-card">
    <h3>오행 상세 분포</h3>
    <div style="display: flex; gap: 0.5rem;">
        <div class="elem-item wood">목(Wood)<br>{{ interp.ohaeng_analysis.percentages.wood }}%</div>
        <div class="elem-item fire">화(Fire)<br>{{ interp.ohaeng_analysis.percentages.fire }}%</div>
        <div class="elem-item earth">토(Earth)<br>{{ interp.ohaeng_analysis.percentages.earth }}%</div>
        <div class="elem-item metal">금(Metal)<br>{{ interp.ohaeng_analysis.percentages.metal }}%</div>
        <div class="elem-item water">수(Water)<br>{{ interp.ohaeng_analysis.percentages.water }}%</div>
    </div>
</div>
```

### 탭 3: 인생의 흐름 (id="gmhs")

```html
<div class="timeline">
    {% for period in ['year', 'month', 'day', 'hour'] %}
    <div class="timeline-item">
        <div class="timeline-content glass-card">
            <div class="timeline-pillar">
                <h3>{{ interp.gmhs[period].period }}</h3>
                <span class="hanja">{{ interp.gmhs[period].pillar.gan }}</span>
                <span class="hanja">{{ interp.gmhs[period].pillar.zhi }}</span>
            </div>
            <div class="timeline-desc">
                <p>{{ interp.gmhs[period].desc }}</p>
            </div>
        </div>
    </div>
    {% endfor %}
</div>
```

### 탭 4: 대운 (id="daewoon")

```html
<!-- 대운 흐름 분석 -->
<div class="glass-card" style="border-left: 5px solid var(--primary);">
    <h3><i class="fa-solid fa-chart-line"></i> 대운의 흐름과 방향성</h3>
    <p>{{ interp.daewoon_trend }}</p>
</div>

<!-- 10년 대운 상세 분석 (8개 카드) -->
<div class="glass-card">
    <h3>10년 대운 상세 분석</h3>
    {% for dw in interp.daewoon %}
    {% set parts = dw.text.split(']') %}
    <div class="dw-card">
        <div class="dw-age">{{ dw.age }}세~</div>
        <div class="dw-pillar">
            <span class="{{ dw.gan_element }}">{{ dw.gan }}</span>
            <span class="{{ dw.zhi_element }}">{{ dw.zhi }}</span>
        </div>
        <h4>{{ parts[0] }}]</h4>
        <p>{{ parts[1] | trim }}</p>
    </div>
    {% endfor %}
</div>
```

---

## 📈 Chart.js 레이더 차트

```javascript
const ohaengData = {{ ohaeng | tojson }};

new Chart(ctx.getContext('2d'), {
    type: 'radar',
    data: {
        labels: ['목 (Wood)', '화 (Fire)', '토 (Earth)', '금 (Metal)', '수 (Water)'],
        datasets: [{
            label: '오행 분포',
            data: [ohaengData.wood, ohaengData.fire, ohaengData.earth, ohaengData.metal, ohaengData.water],
            backgroundColor: 'rgba(167, 139, 250, 0.4)',
            borderColor: '#a78bfa',
            pointBackgroundColor: '#fff',
            pointBorderColor: '#a78bfa',
            borderWidth: 2,
            pointRadius: 4,
            pointHoverRadius: 6
        }]
    },
    options: {
        scales: {
            r: {
                suggestedMin: 0,
                suggestedMax: 5,
                ticks: { display: false }
            }
        },
        plugins: { legend: { display: false } }
    }
});
```

---

## 🔀 탭 전환 JavaScript

```javascript
function openTab(evt, tabName) {
    // 모든 탭 컨텐츠 숨김
    var contents = document.querySelectorAll('.tab-content');
    contents.forEach(function (content) {
        content.style.display = 'none';
        content.classList.remove('active');
    });

    // 모든 탭 버튼 비활성화
    var buttons = document.querySelectorAll('.tab-btn');
    buttons.forEach(function (btn) {
        btn.classList.remove('active');
    });

    // 선택된 탭 활성화
    document.getElementById(tabName).style.display = 'block';
    document.getElementById(tabName).classList.add('active');
    evt.currentTarget.classList.add('active');
}
```
